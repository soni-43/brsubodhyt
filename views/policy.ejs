<%- include('partials/head') %>

<div class="section-header">
  <h2 class="section-title"><%= title %></h2>
</div>

<div class="glass-card">
  <div style="color:var(--text-secondary);font-size:14px;line-height:1.8;white-space:pre-wrap;">
    <%= policies[policyType] || 'Content not available.' %>
  </div>
</div>

<%- include('partials/footer') %>
